import * as React from 'react';
import { View, Text } from 'react-native';
const ResultsScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>inside results screen</Text>
    </View>
  );
};
export default ResultsScreen;
